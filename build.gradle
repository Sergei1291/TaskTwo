plugins {
    id 'war'
    id 'java'
}

group 'com.epam.esm'
version '1.0-SNAPSHOT'

repositories {
    mavenCentral()
}

dependencies {
    compile(project(':repository'))
    compile(project(':service'))
    compile(project(':web'))
}